name: Validation

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  validate:
    uses: InstechSandbox/.github/.github/workflows/reusable-python-validation.yml@ee7c589b6edeb479707d21c3d256092319e7af88
    with:
      python-version: '3.11'
      install-command: 'true'
      validate-command: ./validate_local_build.sh
      cache-dependency-path: app/requirements.txt